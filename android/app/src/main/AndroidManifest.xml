<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Decision 7 (Analytics — ratified 2026-08-31, see docs/BACKEND.md):
         AD_ID / Google Advertising signals are OFF. The `firebase-analytics`
         Android SDK can pull in `com.google.android.gms:play-services-ads-identifier`
         as an optional dependency, which declares this permission in ITS OWN
         manifest; `tools:node="remove"` strips it back out of the merged
         manifest so the app never requests the advertising id at all,
         regardless of what any dependency's manifest asks for. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />

    <!-- android:label is the launcher label, shown under the icon on the
         device home screen. It is independent of the Play Store listing title
         (set in the Play Console) and of the web app's title in
         web/index.html — three separate strings, kept consistent by hand.
         Changed from the lowercase template value "dindin" to the brand
         spelling before the first Play upload. Unlike the package name, this
         one can still be changed in a later release. -->
    <application
        android:label="Dindin"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Decision 7 (Analytics, docs/BACKEND.md): belt-and-suspenders
             alongside the AD_ID permission removal above — even if some
             future dependency re-adds that permission, these two flags keep
             the SDK from collecting the advertising id or personalizing on
             Google signals. -->
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="google_analytics_default_allow_ad_personalization_signals"
            android:value="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
