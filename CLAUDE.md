This repo is mid-migration: the primary app is now **Flutter** (`lib/`, `pubspec.yaml`, Firebase backend). The old **Next.js** app is kept under `next/` until the migration is done — see `FLUTTER_MIGRATION.md` for the full plan and rationale. Rules specific to the Next.js code live in `next/AGENTS.md` and apply only when working inside `next/`.

